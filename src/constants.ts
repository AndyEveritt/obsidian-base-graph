/** Bases view type id. Stored as `type:` in .base files, so never change it after release. */
export const VIEW_TYPE = 'base-graph';

/** Source id registered with the Page preview core plugin. */
export const HOVER_SOURCE = 'base-graph';
